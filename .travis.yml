language: cpp
compiler:
  - gcc
  - clang 
os: linux
before_install:
  - sudo apt-get update 
  - sudo apt-get -y install python3.6
  - sudo apt-get -y install python3.pip
  - pip3 install sympy
install: 
  - bash
script: 
  - g++ Analizador.cpp
  - ./a.out -i Pruebas.txt

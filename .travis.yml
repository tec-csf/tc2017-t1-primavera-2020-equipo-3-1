language: cpp
compiler:
  - gcc
  - clang 
os: linux
install:  
  - sudo apt install python3.6
  - sudo apt-get install python3.pip
  - pip3 install sympy
script: 
  - g++ Analizador.cpp
  - ./a.out -i Pruebas.txt
